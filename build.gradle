import org.codehaus.groovy.control.CompilerConfiguration

/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'uokumura' at '17/05/06 21:21' with Gradle 2.14
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/2.14/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'groovy'

repositories {
    jcenter()
}

dependencies {
}

def GroovyShell() {
  new GroovyShell(new CompilerConfiguration(sourceEncoding: 'utf-8'))
}

task splitInput << {
  GroovyShell().run(file('src/scripts/groovy/SplitInput.groovy'))
}

task splitOutput << {
  GroovyShell().run(file('src/scripts/groovy/SplitOutput.groovy'))
}

task run << {
  println "run"
}

run.dependsOn {
  [splitInput, splitOutput]
}
